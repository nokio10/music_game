
***
# 🎧 ЧО? ЗА? ТРЕК? — Музыкальный Квиз

Веб-приложение для проведения интерактивной игры "Угадай продолжение песни" (или "Угадай мелодию") в реальном времени. Идеально подходит для вечеринок, корпоративов или дружеских посиделок.

![Python](https://img.shields.io/badge/Python-3.9%2B-blue)
![Flask](https://img.shields.io/badge/Flask-SocketIO-green)
![Docker](https://img.shields.io/badge/Docker-Supported-blue)

## ✨ Возможности

*   **Real-time взаимодействие:** Игроки подключаются с телефонов, ведущий управляет игрой с ноутбука.
*   **Два типа вопросов:**
    *   Выбор варианта (1 балл).
    *   Ручной ввод текста (2 балла, нечувствителен к регистру и знакам препинания).
*   **Умная админка:**
    *   Управление воспроизведением музыки.
    *   Автоматический подсчет очков.
    *   Ручная корректировка баллов (+/-).
    *   Мониторинг ответов игроков в реальном времени.
*   **Визуализация:**
    *   Турнирная таблица после каждого раунда.
    *   Анимация салюта для победителя в конце игры.

## 📂 Структура проекта

Для работы игры требуется следующая структура папок и файлов:

```text
.
├── app.py                # Основной код сервера
├── requirements.txt      # Зависимости
├── Dockerfile            # Настройки Docker
├── docker-compose.yml    # Запуск через Docker Compose
├── questions.json        # Сценарий игры и правильные ответы
├── templates/            # HTML шаблоны
│   ├── admin.html
│   └── index.html
└── media/                # Папка с аудиофайлами (создается вами)
    ├── 1-1.mp3           # Вопрос 1 (загадка)
    ├── 1-2.mp3           # Вопрос 1 (ответ/продолжение)
    ├── 2-1.mp3
    └── ...
```

## 🚀 Установка и запуск

### Вариант 1: Запуск через Docker (Рекомендуется)

Если у вас установлен Docker и Docker Compose:

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/ВАШ_НИК/music-game.git
    cd music-game
    ```
2.  Положите свои аудиофайлы в папку `media/` и настройте `questions.json` (см. раздел "Настройка контента").
3.  Запустите контейнер:
    ```bash
    docker-compose up --build
    ```
4.  Игра доступна по адресу: `http://localhost:5000`

### Вариант 2: Локальный запуск (Python)

1.  Установите Python 3.9+.
2.  Клонируйте репозиторий и перейдите в папку.
3.  Создайте виртуальное окружение и активируйте его:
    ```bash
    python -m venv venv
    # Windows:
    venv\Scripts\activate
    # Mac/Linux:
    source venv/bin/activate
    ```
4.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
5.  Запустите приложение:
    ```bash
    python app.py
    ```

## ⚙️ Настройка контента

### 1. Аудиофайлы (`/media`)
Файлы должны лежать в папке `media` и иметь строгое именование:
*   `X-1.mp3` — **Вопрос** (отрывок, который слушают игроки).
*   `X-2.mp3` — **Ответ** (продолжение песни, играет при показе правильного ответа).

Где `X` — это `id` вопроса из файла `questions.json`.

### 2. Сценарий (`questions.json`)
Файл содержит массив объектов вопросов. Пример:

```json
[
  {
    "id": 1,
    "type": "choice",
    "question": "Как заканчивается эта строка?",
    "options": ["Вариант А", "Вариант Б", "Вариант В", "Вариант Г"],
    "answer": "Вариант Б"
  },
  {
    "id": 2,
    "type": "text",
    "question": "Напишите пропущенное слово",
    "options": [],
    "answer": "Любовь"
  }
]
```

*   **id**: Число, должно совпадать с именем файла в папке `media` (1 -> `1-1.mp3`).
*   **type**: `"choice"` (выбор из вариантов) или `"text"` (ввод текста).
*   **options**: Массив строк. Заполняется только для типа `choice`.
*   **answer**: Правильный ответ (строка).

## 🎮 Как играть

1.  **Администратор (Ведущий):**
    *   Заходит по адресу `/admin`.
    *   Логин: `admin`, Пароль: `password` (можно изменить в `app.py`).
    *   Нажимает "Начать новую игру".
    *   Управляет процессом: включает музыку вопроса -> ждет ответов -> показывает ответ -> включает следующий вопрос.

2.  **Игроки:**
    *   Подключаются к одной Wi-Fi сети с ведущим.
    *   Заходят по IP-адресу ведущего (например, `192.168.1.5:5000`).
    *   Вводят имя и ждут начала раунда.
    *   Ввод ответа доступен **только после окончания** воспроизведения трека-вопроса.

## 🔒 Безопасность

По умолчанию в коде (`app.py`) установлены стандартные данные для входа в админку:
```python
ADMIN_USER = "admin"
ADMIN_PASS = "password"
```
**Настоятельно рекомендуется изменить их перед реальной игрой!**

## 🛠 Технологии

*   [Flask](https://flask.palletsprojects.com/) - веб-фреймворк.
*   [Flask-SocketIO](https://flask-socketio.readthedocs.io/) - работа с WebSockets.
*   Bootstrap 5 - интерфейс.
*   HTML5 Audio API - воспроизведение музыки.

---
Приятной игры! 🎵
